# ספריות הליבה של LangChain ואורקסטרציה
langchain==0.3.0
langchain-core==0.3.0
langchain-community==0.3.0

# חיבור למודלים של OpenAI (Embeddings & Chat)
langchain-openai==0.2.0

# בסיס הנתונים הוקטורי (Vector DB)
chromadb==0.5.5

# תמיכה בטעינת קבצי PDF (לשלב הבא של הפרויקט)
pypdf==4.2.0

# ניהול משתני סביבה ומפתחות API דרך קובץ .env
python-dotenv==1.0.1

# כלי עזר לחישוב ואופטימיזציה של אורך הטקסט (Tokens)
tiktoken==0.7.0
fastapi 
uvicorn